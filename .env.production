# Production environment — baked at Docker build time by Vite
# Ces valeurs sont embarquées dans le bundle JS au moment du build
VITE_API_URL=https://api-route.sublymus.com/v1
VITE_SOCKET_URL=https://api-route.sublymus.com
